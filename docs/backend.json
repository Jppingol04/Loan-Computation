
{
  "entities": {
    "Loan": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Loan",
      "type": "object",
      "description": "Represents a single auto loan, storing its primary parameters and metadata.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Loan entity."
        },
        "loanName": {
          "type": "string",
          "description": "User-defined name or reference for the loan."
        },
        "principalAmount": {
          "type": "number",
          "description": "The initial principal amount of the loan."
        },
        "annualInterestRate": {
          "type": "number",
          "description": "The annual interest rate in percentage, used for calculations (e.g., 5.5 for 5.5%)."
        },
        "termMonths": {
          "type": "number",
          "description": "The total duration of the loan in months."
        },
        "startDate": {
          "type": "string",
          "description": "The start date of the loan.",
          "format": "date"
        },
        "currency": {
          "type": "string",
          "description": "The currency code for the loan (e.g., AED, USD, EUR, GBP, PHP)."
        },
        "dayCountConvention": {
          "type": "string",
          "description": "The day-count convention for interest accrual.",
          "enum": ["30/360", "30/365", "ACT/360", "ACT/365"]
        },
        "userId": {
          "type": "string",
          "description": "Reference to the user who owns this loan. (Relationship: User 1:N Loan)"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the loan record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the loan record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "loanName",
        "principalAmount",
        "annualInterestRate",
        "termMonths",
        "startDate",
        "currency",
        "dayCountConvention",
        "userId",
        "createdAt"
      ]
    },
    "AmortizationPeriod": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AmortizationPeriod",
      "type": "object",
      "description": "Represents a single period (month) in the loan's amortization schedule, detailing financial movements.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AmortizationPeriod entity. Typically derived from the period number."
        },
        "loanId": {
          "type": "string",
          "description": "Reference to the parent Loan entity. (Relationship: Loan 1:N AmortizationPeriod)"
        },
        "periodNumber": {
          "type": "number",
          "description": "The sequential number of the amortization period."
        },
        "date": {
          "type": "string",
          "description": "The end date of the amortization period.",
          "format": "date"
        },
        "openingBalance": {
          "type": "number",
          "description": "The outstanding principal balance at the beginning of the period."
        },
        "interestAccrual": {
          "type": "number",
          "description": "The amount of interest accrued during this period based on the opening balance and monthly rate."
        },
        "principalPortion": {
          "type": "number",
          "description": "The portion of the total payment that reduces the principal balance."
        },
        "totalPayment": {
          "type": "number",
          "description": "The total payment made for this period."
        },
        "closingBalance": {
          "type": "number",
          "description": "The outstanding principal balance at the end of the period."
        },
        "status": {
          "type": "string",
          "description": "The status of this period (e.g., projected, paid, recalculated).",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "loanId",
        "periodNumber",
        "date",
        "openingBalance",
        "interestAccrual",
        "principalPortion",
        "totalPayment",
        "closingBalance",
        "status"
      ]
    },
    "InterestRateChange": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "InterestRateChange",
      "type": "object",
      "description": "Records details of an interest rate change applied to a loan, crucial for IFRS 9 prospective recalculation and audit trail.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the InterestRateChange entity."
        },
        "loanId": {
          "type": "string",
          "description": "Reference to the Loan entity to which this rate change applies. (Relationship: Loan 1:N InterestRateChange)"
        },
        "newAnnualRate": {
          "type": "number",
          "description": "The new annual interest rate applied from the effective period."
        },
        "effectiveFromPeriod": {
          "type": "number",
          "description": "The period number from which the new interest rate becomes effective."
        },
        "reasonForChange": {
          "type": "string",
          "description": "Detailed explanation or justification for the interest rate change."
        },
        "oldAnnualRate": {
          "type": "number",
          "description": "The annual interest rate before this change was applied."
        },
        "remainingBalanceAtChange": {
          "type": "number",
          "description": "The opening balance of the loan at the effective period of the rate change, used as the new principal for recalculation."
        },
        "changedAt": {
          "type": "string",
          "description": "Timestamp when the interest rate change was recorded.",
          "format": "date-time"
        },
        "changedByUserId": {
          "type": "string",
          "description": "Reference to the user who initiated this interest rate change. (Relationship: User 1:N InterestRateChange)"
        }
      },
      "required": [
        "id",
        "loanId",
        "newAnnualRate",
        "effectiveFromPeriod",
        "reasonForChange",
        "oldAnnualRate",
        "remainingBalanceAtChange",
        "changedAt",
        "changedByUserId"
      ]
    },
    "JournalEntry": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "JournalEntry",
      "type": "object",
      "description": "Represents a single journal entry or a set of related journal lines for a specific loan period, compatible with ERP systems like Odoo.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the JournalEntry entity."
        },
        "loanId": {
          "type": "string",
          "description": "Reference to the parent Loan entity. (Relationship: Loan 1:N JournalEntry)"
        },
        "periodNumber": {
          "type": "number",
          "description": "The amortization period to which this journal entry relates. (Implicitly references AmortizationPeriod via periodNumber)"
        },
        "journalType": {
          "type": "string",
          "description": "Type of journal entry (e.g., 'Accrual', 'Payment').",
          "items": {
            "type": "string"
          }
        },
        "date": {
          "type": "string",
          "description": "The date of the journal entry.",
          "format": "date"
        },
        "reference": {
          "type": "string",
          "description": "A descriptive reference for the journal entry (e.g., 'LoanName - Period X Accrual')."
        },
        "lines": {
          "type": "array",
          "description": "An array of individual journal entry lines (debits/credits).",
          "items": {
            "type": "string"
          }
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the journal entry was generated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "loanId",
        "periodNumber",
        "journalType",
        "date",
        "reference",
        "lines",
        "createdAt"
      ]
    },
    "AuditEntry": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AuditEntry",
      "type": "object",
      "description": "Records an immutable log of significant actions and changes related to a loan for auditing purposes.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AuditEntry entity."
        },
        "loanId": {
          "type": "string",
          "description": "Reference to the parent Loan entity. (Relationship: Loan 1:N AuditEntry)"
        },
        "timestamp": {
          "type": "string",
          "description": "ISO 8601 timestamp when the action occurred.",
          "format": "date-time"
        },
        "actionType": {
          "type": "string",
          "description": "Categorization of the action (e.g., 'Schedule Generated', 'Interest Rate Change', 'Period Marked Paid', 'Excel Export')."
        },
        "details": {
          "type": "string",
          "description": "A descriptive text providing context for the action."
        },
        "oldValue": {
          "type": "string",
          "description": "A JSON string representation of the data state before the action, if applicable. (e.g., old rate, old status)"
        },
        "newValue": {
          "type": "string",
          "description": "A JSON string representation of the data state after the action, if applicable. (e.g., new rate, new status)"
        },
        "userId": {
          "type": "string",
          "description": "Reference to the user who performed the action. (Relationship: User 1:N AuditEntry)"
        }
      },
      "required": [
        "id",
        "loanId",
        "timestamp",
        "actionType",
        "details",
        "userId"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/loans/{loanId}",
        "definition": {
          "entityName": "Loan",
          "schema": {
            "$ref": "#/backend/entities/Loan"
          },
          "description": "Stores core loan parameters. Includes `userId` for direct ownership-based authorization.",
          "params": [
            {
              "name": "loanId",
              "description": "Unique identifier for the Loan document."
            }
          ]
        }
      },
      {
        "path": "/loans/{loanId}/schedule/{periodNumber}",
        "definition": {
          "entityName": "AmortizationPeriod",
          "schema": {
            "$ref": "#/backend/entities/AmortizationPeriod"
          },
          "description": "Stores a single period's amortization details. Includes denormalized `userId` (loan owner's ID) for authorization independence.",
          "params": [
            {
              "name": "loanId",
              "description": "Unique identifier for the parent Loan document."
            },
            {
              "name": "periodNumber",
              "description": "The sequential number of the amortization period, serving as the document ID."
            }
          ]
        }
      },
      {
        "path": "/loans/{loanId}/interestRateChanges/{changeId}",
        "definition": {
          "entityName": "InterestRateChange",
          "schema": {
            "$ref": "#/backend/entities/InterestRateChange"
          },
          "description": "Stores historical records of interest rate changes for a loan. Includes denormalized `loanOwnerUserId` (loan owner's ID) for authorization independence, in addition to `changedByUserId` (the actor).",
          "params": [
            {
              "name": "loanId",
              "description": "Unique identifier for the parent Loan document."
            },
            {
              "name": "changeId",
              "description": "Unique identifier for the InterestRateChange document."
            }
          ]
        }
      },
      {
        "path": "/loans/{loanId}/journalEntries/{entryId}",
        "definition": {
          "entityName": "JournalEntry",
          "schema": {
            "$ref": "#/backend/entities/JournalEntry"
          },
          "description": "Stores generated journal entries for specific periods of a loan. Includes denormalized `userId` (loan owner's ID) for authorization independence.",
          "params": [
            {
              "name": "loanId",
              "description": "Unique identifier for the parent Loan document."
            },
            {
              "name": "entryId",
              "description": "Unique identifier for the JournalEntry document."
            }
          ]
        }
      },
      {
        "path": "/loans/{loanId}/auditTrail/{entryId}",
        "definition": {
          "entityName": "AuditEntry",
          "schema": {
            "$ref": "#/backend/entities/AuditEntry"
          },
          "description": "Stores immutable audit logs for actions related to a loan. Includes denormalized `loanOwnerUserId` (loan owner's ID) for authorization independence, in addition to `userId` (the actor).",
          "params": [
            {
              "name": "loanId",
              "description": "Unique identifier for the parent Loan document."
            },
            {
              "name": "entryId",
              "description": "Unique identifier for the AuditEntry document."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure for IFRS LoanGuard prioritizes security, scalability, and debuggability by adhering to core design principles, especially Authorization Independence and Query Against Paths (QAPs).\n\n**Authorization Independence via Denormalization:**\nTo achieve critical Authorization Independence, the design meticulously denormalizes the `userId` (representing the loan owner) into every document that is logically owned by or associated with a specific loan. This eliminates the need for expensive and complex `get()` calls within Firestore Security Rules, which would otherwise be required to check a parent document's `userId`. Instead, authorization rules can perform atomic checks directly on the `resource.data` of the document being accessed. For instance:\n1.  **`/loans/{loanId}`:** The `Loan` document itself inherently contains the `userId` of its owner.\n2.  **`/loans/{loanId}/schedule/{periodNumber}` (`AmortizationPeriod`) and `/loans/{loanId}/journalEntries/{entryId}` (`JournalEntry`):** These subcollection documents, while logically children of a `Loan`, do not originally include the loan owner's `userId`. To ensure Authorization Independence, a `userId` field, representing the loan owner, is explicitly added to these documents.\n3.  **`/loans/{loanId}/interestRateChanges/{changeId}` (`InterestRateChange`) and `/loans/{loanId}/auditTrail/{entryId}` (`AuditEntry`):** These entities already define a `userId` or `changedByUserId` field, which signifies the *actor* who performed the action. To avoid ambiguity and explicitly represent the *loan owner* for authorization purposes, a new field, `loanOwnerUserId`, is added to these documents. This clear distinction allows rules to verify `request.auth.uid` against the loan's owner without confusion with the action's performer. \nThis denormalization strategy ensures that a user's ownership can be verified directly on any document, simplifying security rules to `request.auth.uid == resource.data.userId` (or `loanOwnerUserId`), making them highly robust, performant, and easily debuggable.\n\n**Support for Query Against Paths (QAPs):**\nThe structural design, coupled with denormalized authorization fields, fully supports QAPs, ensuring secure and efficient list operations:\n1.  **Top-Level Queries (`/loans`):** Users can directly query for all loans they own by filtering on the `userId` field: `db.collection('loans').where('userId', '==', request.auth.uid)`. This query pattern is highly optimized by Firestore (requiring an index on `userId`) and is inherently secure because the security rules enforce `resource.data.userId == request.auth.uid` for every document returned, preventing unauthorized access.\n2.  **Subcollection Queries:** Once a user has identified a `loanId` they own (e.g., from a secure top-level query), they can then query its subcollections (e.g., `db.collection('loans').doc(loanId).collection('schedule')`). Even in these nested contexts, the presence of the denormalized `userId` (or `loanOwnerUserId`) in *every subcollection document* provides an additional layer of explicit security. This means that if a user somehow attempts to bypass the parent loan's authorization and directly queries a subcollection document with an `loanId` they don't own, the security rule for that subcollection document would independently fail the `resource.data.userId == request.auth.uid` check. This prevents data leakage and ensures that all `list` operations, whether on top-level collections or subcollections, are implicitly or explicitly filtered and secured by the authenticated user's UID.\n\n**Structural Segregation and Consistency:**\nAll data within the `loans` collection and its subcollections shares a homogeneous security posture: ownership by a single user. This eliminates the need for complex conditional logic in rules based on data types or public/private flags within the same collection. The use of descriptive wildcards (e.g., `{loanId}`, `{periodNumber}`, `{changeId}`, `{entryId}`) and standardized authorization field names ensures clarity and predictability across the data model."
  }
}
